export { colors } from './colors';
export { spacing } from './spacing';

/** Marge basse pour contenu au-dessus de la tab bar flottante (safe area + capsule). */
export const TAB_BAR_FLOAT_BOTTOM_OFFSET = 100;
